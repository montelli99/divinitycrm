# This directory contains secret configuration files.
# NEVER commit these to Git. They are already in .gitignore.
# 
# If you lose a key, check:
# 1. divinitycrm/SECRETS.env (project secrets)
# 2. ../SECRETS_BACKUP.env (master backup in parent dir)
# 3. Your browser password manager (if saved)
# 4. The respective service dashboard ( regenerate if needed)
